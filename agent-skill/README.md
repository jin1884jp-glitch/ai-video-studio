# Agent skills

Place reusable skills in this directory. Each skill should normally have its own folder containing a `SKILL.md` file.

Example:

```text
agent-skill/
└── example-skill/
    └── SKILL.md
```
